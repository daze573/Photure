<% if post.favorited_by?(current_user) %>
  <%= link_to post_favorites_path(post), method: :destroy, remote: true do %>
    <span style="color:red;">
      <i class="fas fa-heart"></i><%= post.favorites.count %>
    </span>
  <% end %>
<% else %>
  <%= link_to post_favorites_path(post), method: :create, remote: true do %>
    <i class="fas fa-heart"></i><%= post.favorites.count %>
  <% end %>
<% end %>
